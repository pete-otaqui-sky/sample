platform: linux
inputs:
  - name: src
outputs:
  - name: ref
image_resource:
  type: docker-image
  source: { repository: alpine }
run:
  path: /bin/sh
  args:
    - -exc
    - |
      #!/bin/sh
      set -e
      set +x

      cat src/.git/ref > ref/ref
